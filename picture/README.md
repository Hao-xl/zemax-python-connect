# picture

Place user-facing screenshots here before publishing the skill to GitHub.

Recommended filenames:

- `interactive-extension-button.png`: OpticStudio `Programming / 编程` tab showing the independent `交互扩展 / Interactive Extension` button in the `ZOS-API.NET` area.
- `interactive-extension-waiting-dialog.png`: the waiting dialog showing `Instance Number`, `Status: waiting for connection`, `Auto Close on Disconnect`, `Terminate`, and `Close`.

The skill and README reference these screenshots as visual confirmation for Interactive Extension mode. If the image names differ, update the links in `README.md`.
